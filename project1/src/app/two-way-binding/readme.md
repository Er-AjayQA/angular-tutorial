## What is 2 way binding?

# Two-way binding is a technique that automatically synchronizes data between a model (business logic/data/ts file) and a view (UI/html file), so that when either changes, the other updates immediately.
